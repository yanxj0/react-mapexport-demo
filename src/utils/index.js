import { loadMap, TMap} from './mapUtil';
import { getBase64Img, getTemplateInnerSize, downLoad, getContainerSize } from './calcUtil';
import print from './printUtil';

export default  { loadMap, TMap, getBase64Img, getTemplateInnerSize, downLoad, getContainerSize, print }